name: "resolve-tags"
shortDesc: "resolve images in kubernetes yamls from tags to digests"
command: "./resolve-tags $PWD -f=$KUBECTL_PLUGINS_LOCAL_FLAG_FILENAME"
flags:
  - name: "filename"
    shorthand: "f"
    desc: "files to be resolved"

